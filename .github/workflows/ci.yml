name: CI

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]

jobs:
  contract:
    name: Validate skill-set contract
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Run contract validation
        run: python3 scripts/validate_wiki_contract.py
